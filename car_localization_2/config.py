"""
========== 小车定位系统 - 配置文件 ==========
修改此文件后重新运行 main.py 即可生效。
所有坐标单位为毫米(mm)。
"""

# ============================================================
# 摄像头设置
# ============================================================
CAMERA_INDEX = 1          # USB 外接摄像头通常为 0 或 1
CAMERA_WIDTH = 1920       # 期望分辨率（实际取决于硬件）
CAMERA_HEIGHT = 1080
CAMERA_FPS = 60           # 期望帧率（实际取决于硬件是否支持）

# ============================================================
# AprilTag 设置
# ============================================================
TAG_FAMILY = "tagStandard41h12"   # 推荐此家族，可改为 tag36h11 等

# ============================================================
# 场地 - 四个角落 tag 的配置
# ============================================================
# 坐标系说明：
#   X 轴 → 右
#   Y 轴 → 下（图像坐标系）
#   原点默认在左上角 (top_left) 位置
#
# 每个角落需要配置：
#   - id: 贴在该角落的 AprilTag 的 ID
#   - x, y: 该角落在现实世界中的物理坐标 (mm)
#
# 打印好 tag 后，将对应的 ID 填入下方即可：

CORNER_TAGS = {
    "top_left":     {"id": 1, "x": 500.0,    "y": 500.0},
    "top_right":    {"id": 2, "x": 500.0, "y": 4500.0},
    "bottom_left":  {"id": 3, "x": 3700.0,    "y": 1000.0},
    "bottom_right": {"id": 4, "x": 3700.0, "y": 4000.0},
}

# ============================================================
# 小车 tag 配置
# ============================================================
CAR_TAG_ID = 0             # 贴在小车上的 AprilTag 的 ID

# ============================================================
# 显示设置
# ============================================================
SHOW_GRID = False           # 是否在图像上绘制场地坐标网格
GRID_SPACING = 1000         # 网格间距 (mm)
DISPLAY_SCALE = 0.5         # 显示缩放比例（0.5 = 缩小到 50%，避免 4K 画面超出屏幕）

# ============================================================
# UDP 发送设置（将小车坐标发送到目标设备）
# ============================================================
UDP_ENABLED = True                 # 是否启用 UDP 发送
UDP_TARGET_IP = "192.168.38.7"     # 目标设备 IP 地址
UDP_TARGET_PORT = 9005             # 目标设备端口
UDP_SEND_INTERVAL = 0.018           # 发送间隔（秒），0.05 = 每秒 20 次
